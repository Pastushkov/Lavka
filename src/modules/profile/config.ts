export interface IStartValues {
    email: string
    name: string
    phone:string
    promToken: string
}

export const startValues: IStartValues = {
    email: '',
    name: '',
    phone: '',
    promToken: ''
}